name: Daily Stock Signal

on:
  schedule:
    # 20:30 Asia/Taipei, Monday-Friday.
    - cron: "30 12 * * 1-5"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Sync market data
        run: python sync_today.py --workers 6 --mode auto

      - name: Build reports and send Telegram
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          ALERT_MAX_ITEMS: "30"
        run: python alert_signals.py --channel telegram --chart-items 4 --max-items 30 --send-empty

      - name: Build static site
        shell: pwsh
        run: ./build_site.ps1

      - name: Commit generated data and reports
        shell: bash
        run: |
          shopt -s nullglob
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          files=(
            data/all_twse/*.csv
            data/all_tpex/*.csv
            reports/daily_signal_alert.json
            reports/daily_signal_alert.csv
            reports/daily_signal_alert.txt
            charts/daily_alert/*.png
          )
          if [ "${#files[@]}" -gt 0 ]; then
            git add -f "${files[@]}"
          fi
          git add .github/workflows/daily-signal.yml build_site.ps1 requirements.txt
          if git diff --cached --quiet; then
            echo "No generated changes to commit."
          else
            git commit -m "Update daily stock signals"
            git push
          fi

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy site --project-name daily-stock-signal
